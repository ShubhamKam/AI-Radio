FROM node:20-alpine

WORKDIR /app

# Install system dependencies
RUN apk add --no-cache python3 make g++ ffmpeg postgresql-client

# Install nodemon globally for hot reload
RUN npm install -g nodemon tsx

EXPOSE 3000

CMD ["npm", "run", "dev"]
