{
  "name": "ai-radio",
  "private": true,
  "version": "0.1.0",
  "description": "AI Radio: ingest content + curate + generate radio shows with music",
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "format": "pnpm -r format",
    "typecheck": "pnpm -r typecheck"
  },
  "packageManager": "pnpm@10.27.0",
  "pnpm": {
    "onlyBuiltDependencies": ["@prisma/client", "@prisma/engines", "@swc/core", "esbuild", "prisma"]
  },
  "license": "MIT"
}
